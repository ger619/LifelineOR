<nav class="flex">
  <%= render 'side' %>
  <div class="w-full p-2 mx-auto">
    <div class="max-w-full mx-auto mt-3">
        <%= form_with(model: Chat.new) do |f| %>
          <%= f.submit 'New Submit', class: "text-white end-2.5 bottom-2 top-2 bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"%>
        <% end %>


      <%= render partial: @chat if @chat.present? %>
    </div>
  </div>


</nav>